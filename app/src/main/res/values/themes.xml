<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="DataScanner20" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSplashScreenBackground">@color/logo</item>
        <item name="android:windowSplashScreenAnimationDuration">5000</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_screen_logo</item>
        <item name="splashScreenIconSize">100dp</item>
        <item name="android:statusBarColor">@color/logo</item>
        <item name="colorPrimaryDark">@color/logo</item>

    </style>
</resources>